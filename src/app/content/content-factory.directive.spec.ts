import { ContentFactoryDirective } from './content-factory.directive';

describe('ContentFactoryDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentFactoryDirective();
    expect(directive).toBeTruthy();
  });
});
